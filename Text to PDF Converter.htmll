<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Free Text to PDF Converter | Online Tool</title>
    <meta name="description" content="Convert any text to PDF instantly with our free online tool. Works on all devices. No registration required!">
    <meta name="keywords" content="text to pdf, convert text to pdf, free pdf converter, online pdf tool, create pdf from text">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.9)), url('https://images.unsplash.com/photo-1509228468518-180dd4864904?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80') no-repeat center center fixed;
            background-size: cover;
            color: #fff;
            line-height: 1.6;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            flex: 1;
        }
        
        header {
            text-align: center;
            padding: 30px 0;
        }
        
        .logo {
            font-size: 2.5rem;
            margin-bottom: 10px;
            color: #4CAF50;
        }
        
        h1 {
            font-size: 2.8rem;
            margin-bottom: 15px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }
        
        .subtitle {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 700px;
            margin: 0 auto 30px;
        }
        
        .converter-container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            margin-bottom: 40px;
        }
        
        .converter-row {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
            margin-bottom: 30px;
        }
        
        .input-section, .output-section {
            flex: 1;
            min-width: 300px;
        }
        
        h2 {
            font-size: 1.8rem;
            margin-bottom: 20px;
            color: #4CAF50;
        }
        
        textarea {
            width: 100%;
            height: 250px;
            padding: 15px;
            border-radius: 8px;
            border: 2px solid #4CAF50;
            background: rgba(0, 0, 0, 0.4);
            color: #fff;
            font-size: 16px;
            resize: vertical;
            transition: border-color 0.3s;
        }
        
        textarea:focus {
            outline: none;
            border-color: #8BC34A;
            box-shadow: 0 0 10px rgba(139, 195, 74, 0.5);
        }
        
        .preview-box {
            height: 250px;
            background: rgba(0, 0, 0, 0.4);
            border: 2px solid #2196F3;
            border-radius: 8px;
            padding: 15px;
            overflow-y: auto;
            font-size: 16px;
        }
        
        .controls {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
            margin: 30px 0;
        }
        
        .btn {
            padding: 15px 30px;
            font-size: 1.1rem;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: transform 0.3s, background 0.3s;
            font-weight: 600;
        }
        
        .btn-primary {
            background: #4CAF50;
            color: white;
        }
        
        .btn-secondary {
            background: #2196F3;
            color: white;
        }
        
        .btn:disabled {
            background: #666;
            cursor: not-allowed;
            opacity: 0.6;
        }
        
        .btn:hover:not(:disabled) {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
        
        .btn:active:not(:disabled) {
            transform: translateY(1px);
        }
        
        .features {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
            margin: 50px 0;
        }
        
        .feature-card {
            flex: 1;
            min-width: 250px;
            background: rgba(255, 255, 255, 0.05);
            padding: 25px;
            border-radius: 10px;
            text-align: center;
            transition: transform 0.3s;
        }
        
        .feature-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.1);
        }
        
        .feature-icon {
            font-size: 2.5rem;
            color: #4CAF50;
            margin-bottom: 20px;
        }
        
        .feature-title {
            font-size: 1.4rem;
            margin-bottom: 15px;
        }
        
        .seo-content {
            background: rgba(255, 255, 255, 0.05);
            padding: 40px;
            border-radius: 15px;
            margin: 50px 0;
        }
        
        .seo-content h2 {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .content-columns {
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
        }
        
        .content-col {
            flex: 1;
            min-width: 300px;
        }
        
        .content-col h3 {
            color: #4CAF50;
            margin-bottom: 15px;
            font-size: 1.5rem;
        }
        
        .content-col p {
            margin-bottom: 20px;
        }
        
        .status-message {
            text-align: center;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            font-weight: 500;
            display: none;
        }
        
        .success {
            background: rgba(76, 175, 80, 0.3);
            color: #8BC34A;
            display: block;
        }
        
        .error {
            background: rgba(244, 67, 54, 0.3);
            color: #FF9800;
            display: block;
        }
        
        footer {
            text-align: center;
            padding: 30px 0;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            margin-top: 40px;
        }
        
        .footer-links {
            display: flex;
            justify-content: center;
            gap: 25px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        
        .footer-links a {
            color: #4CAF50;
            text-decoration: none;
            transition: color 0.3s;
        }
        
        .footer-links a:hover {
            color: #8BC34A;
        }
        
        @media (max-width: 768px) {
            h1 {
                font-size: 2.2rem;
            }
            
            .converter-container {
                padding: 20px;
            }
            
            .btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">
                <i class="fas fa-file-pdf"></i>
            </div>
            <h1>Text to PDF Converter</h1>
            <p class="subtitle">Convert any text to PDF instantly with our free online tool. No registration required! Works perfectly on mobile, tablet, and desktop devices.</p>
        </header>
        
        <main>
            <div class="converter-container">
                <div class="converter-row">
                    <div class="input-section">
                        <h2><i class="fas fa-keyboard"></i> Enter Your Text</h2>
                        <textarea id="text-input" placeholder="Type or paste your text here...">Welcome to our free Text to PDF converter! 

Simply enter your text in this box, click the "Convert to PDF" button, and download your PDF file instantly. 

Our tool preserves your formatting and works on all devices. Try it now!

Features:
- Fast PDF generation
- Preserves line breaks
- Responsive design
- Open source technology
- No file size limits</textarea>
                    </div>
                    
                    <div class="output-section">
                        <h2><i class="fas fa-file-pdf"></i> PDF Preview</h2>
                        <div class="preview-box" id="pdf-preview">
                            <h3>PDF Preview</h3>
                            <p>After conversion, your PDF content will appear here.</p>
                            <p>This tool supports:</p>
                            <ul>
                                <li>Paragraphs and line breaks</li>
                                <li>Basic text formatting</li>
                                <li>Special characters</li>
                                <li>Page breaks</li>
                            </ul>
                            <p>For best results, ensure your text is properly formatted before conversion.</p>
                        </div>
                    </div>
                </div>
                
                <div id="status-message" class="status-message"></div>
                
                <div class="controls">
                    <button id="convert-btn" class="btn btn-primary">
                        <i class="fas fa-file-download"></i> Convert to PDF
                    </button>
                    <button id="download-btn" class="btn btn-secondary" disabled>
                        <i class="fas fa-download"></i> Download PDF
                    </button>
                    <button id="clear-btn" class="btn">
                        <i class="fas fa-redo"></i> Clear Text
                    </button>
                </div>
            </div>
            
            <div class="features">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-mobile-alt"></i>
                    </div>
                    <h3 class="feature-title">Fully Responsive</h3>
                    <p>Works perfectly on all devices - smartphones, tablets, laptops, and desktops.</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-lock"></i>
                    </div>
                    <h3 class="feature-title">Secure & Private</h3>
                    <p>Your text is never stored on our servers. All conversions happen in your browser.</p>
                </div>
                
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-bolt"></i>
                    </div>
                    <h3 class="feature-title">Lightning Fast</h3>
                    <p>Convert text to PDF in seconds with our optimized processing engine.</p>
                </div>
            </div>
            
            <div class="seo-content">
                <h2>Why Convert Text to PDF?</h2>
                <div class="content-columns">
                    <div class="content-col">
                        <h3>Preserve Document Integrity</h3>
                        <p>PDF files maintain your original formatting across all devices and operating systems. Unlike text files, PDFs ensure that your document appears exactly as you intended, regardless of the software or device used to view it.</p>
                        
                        <h3>Professional Document Sharing</h3>
                        <p>PDF is the standard format for professional document exchange. Converting your text to PDF gives your documents a polished, professional appearance that's ideal for business communications, academic submissions, and official documentation.</p>
                    </div>
                    
                    <div class="content-col">
                        <h3>Enhanced Security</h3>
                        <p>PDF files can be password-protected and encrypted, providing an additional layer of security for sensitive information. You can control who can view, edit, or print your documents.</p>
                        
                        <h3>Universal Compatibility</h3>
                        <p>PDF files can be opened on any device - Windows, macOS, Linux, iOS, and Android - without requiring specialized software. All modern web browsers can display PDF documents directly.</p>
                    </div>
                </div>
            </div>
        </main>
        
        <footer>
            <p>© 2023 Text to PDF Converter</p>
            <p>This tool uses jsPDF, an open-source JavaScript library for PDF generation.</p>
        </footer>
    </div>

    <script>
        // Initialize jsPDF
        const { jsPDF } = window.jspdf;
        let generatedPDF = null;
        
        // DOM Elements
        const textInput = document.getElementById('text-input');
        const convertBtn = document.getElementById('convert-btn');
        const downloadBtn = document.getElementById('download-btn');
        const clearBtn = document.getElementById('clear-btn');
        const statusMessage = document.getElementById('status-message');
        const pdfPreview = document.getElementById('pdf-preview');
        
        // Convert text to PDF
        convertBtn.addEventListener('click', function() {
            const text = textInput.value.trim();
            
            if (!text) {
                showStatus('Please enter some text to convert', 'error');
                return;
            }
            
            try {
                // Create a new PDF document
                const doc = new jsPDF();
                
                // Set document properties
                doc.setProperties({
                    title: 'Converted Text',
                    subject: 'Text to PDF Conversion',
                    author: 'Text to PDF Converter',
                    keywords: 'text, pdf, conversion'
                });
                
                // Split text into lines
                const lines = doc.splitTextToSize(text, 180);
                
                // Add text to PDF (starting at position 20,20)
                doc.text(lines, 20, 20);
                
                // Store the generated PDF
                generatedPDF = doc;
                
                // Update preview
                pdfPreview.innerHTML = `
                    <h3>Your PDF Content</h3>
                    <p>${text.substring(0, 300)}${text.length > 300 ? '...' : ''}</p>
                    <div class="success" style="padding: 10px; margin-top: 15px; border-radius: 5px;">
                        <i class="fas fa-check-circle"></i> PDF successfully generated!
                    </div>
                    <p style="margin-top: 15px;">Pages: 1 | Characters: ${text.length}</p>
                `;
                
                // Enable download button
                downloadBtn.disabled = false;
                
                showStatus('PDF successfully generated! Click "Download PDF" to save.', 'success');
            } catch (error) {
                console.error('PDF generation error:', error);
                showStatus('Error generating PDF. Please try again.', 'error');
            }
        });
        
        // Download PDF
        downloadBtn.addEventListener('click', function() {
            if (generatedPDF) {
                generatedPDF.save('converted-text.pdf');
                showStatus('PDF downloaded successfully!', 'success');
            } else {
                showStatus('No PDF available to download. Please convert text first.', 'error');
            }
        });
        
        // Clear text
        clearBtn.addEventListener('click', function() {
            textInput.value = '';
            pdfPreview.innerHTML = `
                <h3>PDF Preview</h3>
                <p>After conversion, your PDF content will appear here.</p>
                <p>This tool supports:</p>
                <ul>
                    <li>Paragraphs and line breaks</li>
                    <li>Basic text formatting</li>
                    <li>Special characters</li>
                    <li>Page breaks</li>
                </ul>
                <p>For best results, ensure your text is properly formatted before conversion.</p>
            `;
            downloadBtn.disabled = true;
            generatedPDF = null;
            showStatus('Text cleared. Ready for new input.', 'success');
        });
        
        // Show status message
        function showStatus(message, type) {
            statusMessage.textContent = message;
            statusMessage.className = 'status-message ' + type;
            
            // Clear message after 5 seconds
            setTimeout(() => {
                statusMessage.className = 'status-message';
                statusMessage.textContent = '';
            }, 5000);
        }
        
        // Initialize with a status message
        window.addEventListener('DOMContentLoaded', () => {
            showStatus('Enter your text and click "Convert to PDF" to begin', 'success');
        });
    </script>
</body>
</html>
